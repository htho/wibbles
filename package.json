{
  "name": "wibbles",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-schema": "npm run generate-schema:tileset && npm run generate-schema:level",
    "generate-schema:level": "ts-json-schema-generator --path ./level-schema.ts --type Level --out level.schema.json",
    "generate-schema:tileset": "ts-json-schema-generator --path ./tileset-schema.ts --type Tileset --out tileset.schema.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-json-schema-generator": "^1.0.0",
    "typescript": "^4.6.4"
  }
}
